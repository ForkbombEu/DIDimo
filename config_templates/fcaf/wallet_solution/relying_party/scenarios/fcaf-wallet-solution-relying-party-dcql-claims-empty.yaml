name: FCAF wallet relying-party DCQL claims empty
runtime:
  global_device_id: forkbomb-bv-andrea/usb/device
  temporal:
    activity_options:
      schedule_to_close_timeout: 50m
      start_to_close_timeout: 50m
      retry_policy:
        maximum_attempts: 1

steps:
  - id: evidence-empty-claims-request
    use: json-parse
    with:
      rawJSON: '{"dcql_query":{"credentials":[{"id":"pididentity01","format":"dc+sd-jwt","meta":{"vct_values":["urn:eudi:pid:1"]},"claims":[]}]}}'
      struct_type: map

  - id: reject-empty-claims
    use: mobile-automation
    with:
      version_id: installed_from_external_source
      action_code: |
        appId: eu.europa.ec.euidi
        ---
        - extendedWaitUntil:
            visible: "Home|Documents|Welcome, .*"
            timeout: 30000
        - evalScript: ${output.dcqlQuery = JSON.stringify({credentials:[{id:'pididentity01',format:'dc+sd-jwt',meta:{vct_values:['urn:eudi:pid:1']},claims:[]}]})}
        - evalScript: ${output.authorizationRequest = 'eudi-openid4vp://?client_id=redirect_uri%3Ahttps%3A%2F%2Fexample.com%2Ffcaf-response&response_uri=https%3A%2F%2Fexample.com%2Ffcaf-response&response_type=vp_token&response_mode=direct_post&nonce=fcaf-083-claims-empty&dcql_query=' + encodeURIComponent(output.dcqlQuery)}
        - openLink:
            link: ${output.authorizationRequest}
            autoVerify: false
            browser: false
        - extendedWaitUntil:
            visible: "Oups! Something went wrong"
            timeout: 30000
        - takeScreenshot: fcaf-083-claims-empty-rejected
        - back
        - waitForAnimationToEnd:
            timeout: 2000

  - id: run-fcaf-validators
    use: fcaf-validation
    with:
      test_ids:
        - WS_RP_MS_ProtocolMessages__083
      suite: wallet_solution/relying_party
      pipeline_outputs:
        pipeline.dcql.claims-empty:
          output:
            dcql_exchange: ${{ reject-empty-claims.outputs.body }}
            visual_evidence: ${{ reject-empty-claims.outputs.flow_output.maestro_screenshot_urls }}
            evidence-empty-claims-request: ${{ evidence-empty-claims-request.outputs }}
            reject-empty-claims: ${{ reject-empty-claims.outputs }}
