name: FCAF wallet relying-party DCQL mso_mdoc format
runtime:
  global_device_id: forkbomb-bv-andrea/usb/device
  temporal:
    activity_options:
      schedule_to_close_timeout: 50m
      start_to_close_timeout: 50m
      retry_policy:
        maximum_attempts: 1

steps:
  - id: create-presentation
    use: http-request
    with:
      method: POST
      url: ${fixture.verifier_url}/ui/presentations
      expected_status: 200
      headers:
        Content-Type: application/json
      body:
        type: vp_token
        nonce: fcaf-dcql-format-mso-mdoc
        dcql_query:
          credentials:
            - id: pid_mdoc
              format: mso_mdoc
              meta:
                doctype_value: eu.europa.ec.eudi.pid.1
              claims:
                - path:
                    - eu.europa.ec.eudi.pid.1
                    - given_name
                - path:
                    - eu.europa.ec.eudi.pid.1
                    - family_name

  - id: present-with-reference-implementation
    use: mobile-automation
    with:
      version_id: installed_from_external_source
      parameters:
        CLIENT_ID: ${{ create-presentation.outputs.body.client_id }}
        REQUEST_URI: ${{ create-presentation.outputs.body.request_uri }}
      action_code: |
        appId: eu.europa.ec.euidi
        ---
        - launchApp:
            stopApp: false
            clearState: false
            permissions:
              camera: allow
              notifications: allow
              all: unset
        - evalScript: ${output.authorizationRequest = 'eudi-openid4vp://?client_id=' + encodeURIComponent(env.CLIENT_ID) + '&request_uri=' + encodeURIComponent(env.REQUEST_URI)}
        - openLink:
            link: ${output.authorizationRequest}
            autoVerify: false
            browser: false
        - waitForAnimationToEnd:
            timeout: 5000
        - tapOn:
            text: "(?i)(continue|accept|authorize|share|present)"
            optional: true
        - waitForAnimationToEnd:
            timeout: 5000
        - tapOn:
            text: "(?i)(continue|accept|authorize|share|present)"
            optional: true
        - waitForAnimationToEnd:
            timeout: 5000
        - takeScreenshot: fcaf-dcql-format-mso-mdoc

  - id: get-presentation-result
    use: http-request
    with:
      method: GET
      url: ${fixture.verifier_url}/ui/presentations/${{ create-presentation.outputs.body.transaction_id }}
      expected_status: 200

  - id: run-fcaf-validators
    use: fcaf-validation
    with:
      test_ids:
        - WS_RP_MS_ProtocolMessages__067
      suite: wallet_solution/relying_party
      pipeline_outputs:
        pipeline.dcql.format-mso-mdoc:
          output:
            dcql_exchange: ${{ get-presentation-result.outputs.body }}
            visual_evidence: ${{ present-with-reference-implementation.outputs.flow_output.maestro_screenshot_urls }}
            create-presentation: ${{ create-presentation.outputs }}
            present-with-reference-implementation: ${{ present-with-reference-implementation.outputs }}
            get-presentation-result: ${{ get-presentation-result.outputs }}
