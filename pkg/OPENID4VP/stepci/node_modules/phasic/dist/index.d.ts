export declare type Phase = {
    duration: number;
    arrivalRate: number;
};
export declare type PhaseFunction = {
    (): Promise<any>;
};
export declare function runPhases<T>(phases: Phase[], func: PhaseFunction): Promise<PromiseSettledResult<T>[]>;
